# Data Source Properties
spring.datasource.url=jdbc:postgresql://localhost:5432/tbp_new
spring.datasource.username=postgres
spring.datasource.password=Zagy1991

# JPA properties
spring.jpa.show-sql=true
spring.jpa.generate-ddl=false
spring.jpa.hibernate.ddl-auto=update
spring.data.jpa.repositories.enabled=true
spring.jpa.properties.hibernate.dialect=hr.foi.tbp.hibernate.CustomPostgreSqlDialect

# Security
security.require-ssl=true

# MVC
spring.mvc.view.prefix:/
spring.mvc.view.suffix:.jsp

# HTTP encoding (HttpEncodingProperties)
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true

# Server
server.port = 8443
server.ssl.keyStore=classpath:keystore.jks
server.ssl.keyStorePassword=Zagy1991
server.ssl.keyStoreType=JKS
server.ssl.keyAlias=tomcat